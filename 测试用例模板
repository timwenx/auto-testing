# 测试用例：新增规则并验证列表显示

## 基本信息
| 属性 | 值 |
|------|----|
| 用例名称 | 新增规则并验证列表显示 |
| 优先级 | P0 |
| 测试类型 | 功能测试 |
| 目标页面 | 规则管理页面 |
| 版本 | 5 |

## 描述
测试通过用户界面新增一条规则，规则条件设置为数据对象字段acct.login==xxx，并验证规则保存成功、在规则列表中正确显示，包括规则代号、名称和描述。

## 前置条件
1. 用户已使用测试账号登录SSO（admin/test123456）。
2. 用户拥有规则管理权限。
3. 规则管理页面可正常访问（https://xxxxcom/test/query）。

## 测试步骤
1. 导航到规则管理查询页面。
2. 点击页面上的"新增规则"按钮。
3. 在弹出的规则配置表单中：
   a. 输入规则名称，例如"acct.login==xxx测试"。
   b. 输入规则描述，包含"acct.login==xxx"。
   c. 在"数据对象字段(数据对象 - 字段 > 值)"区域：
      - 在"数据对象"下拉框中选择"acct"
      - 在"字段"下拉框中选择"login"
      - 在"条件"下拉框中选择"等于"
      - 在"值"输入框中输入"xxx"
4. 点击"存檔"按钮保存规则。

## 预期结果
1. 保存操作后，页面顶部显示成功提示消息"規則新增成功！"。
2. 系统自动返回规则列表页面，列表已刷新。
3. 列表顶部显示新增的规则条目，该条目显示：
   - 规则代号：存在（如AT001）
   - 规则名称：匹配输入的名称（如"acct.login==xxx测试"）
   - 规则描述：包含指定描述
   - 数据对象：acct
   - 字段：login
   - 条件：等于
   - 值：xxx
4. 规则状态显示为"启用"。
